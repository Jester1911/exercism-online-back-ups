#pragma once
#include <string>
#include <cctype>
#include <cmath>
#include <vector>

namespace hexadecimal {

    unsigned int hexDigitToDec(char c); 

    unsigned int convert(const char* input);

}  // namespace hexadecimal
